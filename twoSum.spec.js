const { expect } = require('chai');
const twoSum = require('./twoSum');

describe ('twoSum', function() {
  it ('accepts an array and target as an argument')

  it( 'returns an array')

  it( "returns an empty array if an empty array or target is passed in")

}
